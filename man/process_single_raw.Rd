% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_single_raw.R
\name{process_single_raw}
\alias{process_single_raw}
\title{Process a single raw Lotek TXT file exported by WHS Host}
\usage{
process_single_raw(
  raw_file,
  input_path,
  output_path,
  output_prefix,
  allowable_tagcodes,
  tz
)
}
\arguments{
\item{raw_file}{Character string giving the name of the input TXT file.}

\item{input_path}{Path to the folder containing \code{raw_file}.}

\item{output_path}{Path where the processed CSV file should be saved.}

\item{output_prefix}{Optional prefix added to the output file name.}

\item{allowable_tagcodes}{Character vector of hexadecimal tag codes to retain,
or \code{NULL} to keep all tag detections.}

\item{tz}{Character string giving the timezone of the receiver clock.}
}
\value{
Writes a CSV file to \code{output_path}. Returns \code{NULL} (invisible).
}
\description{
Processes a single .TXT file produced by Lotek's WHS Host batch conversion
tool. These files are converted from encrypted .JST receiver files into
human-readable text. This function reformats the content, converts tag IDs,
optionally filters to a set of allowable tag codes, and writes a cleaned CSV
to disk. Because these files may be large, this function is intended to be
run independently for each input file (and can be parallelized).
}
\examples{
\dontrun{
raw_file <- "Example_Lotek_Raw.TXT"
process_single_raw(
  raw_file = raw_file,
  input_path = system.file("extdata", package = "LotekFilter"),
  output_path = tempdir(),
  output_prefix = "Processed_",
  allowable_tagcodes = c("1A2C"),
  tz = "Etc/GMT+8"
)
}

}
